# EmailJS (https://www.emailjs.com/) — browser send, no mailto / no backend required.
# Create a service + template; template fields should include at least: subject, message, user_email (optional).
VITE_EMAILJS_PUBLIC_KEY=
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
